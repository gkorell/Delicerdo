{% extends 'TodoCerdoTodoCerdoBundle::layout.html.twig' %}

{% block body %}

<header>
    <h2>Alta de Recetas</h2>
</header>

    <p>Datos de la receta</p>

<form class="blogger" action="{{ path('receta_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
   
    <input type="submit" class="button" value="Guardar" />
  
</form>

<ul class="record_actions">
    <li>
        <a class="link_navegacion" href="{{ path('receta') }}">
            Volver al listado
        </a>
    </li>
</ul>
{{ tinymce_init() }}    
{% endblock %}
